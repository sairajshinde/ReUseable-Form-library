import { CustomTableDirective } from './custom-table.directive';

describe('CustomTableDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTableDirective();
    expect(directive).toBeTruthy();
  });
});
