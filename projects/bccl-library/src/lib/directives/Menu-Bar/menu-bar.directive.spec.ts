import { MenuBarDirective } from './menu-bar.directive';

describe('MenuBarDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuBarDirective();
    expect(directive).toBeTruthy();
  });
});
